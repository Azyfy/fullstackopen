import React from "react"

const Notification = ({ message }) => (
       <div style={{ color: "blue" }}>
            <p>
                {message}
            </p>
        </div>

)

export default Notification